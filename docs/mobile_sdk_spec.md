# Spirit Mobile Edge SDK Specification

## Overview
The mobile SDK runs on the user's device, collects raw UsageStats (Android) or ScreenTime (iOS), aggregates locally for privacy, and transmits behavioral summaries to Spirit's ingestion API.

## Architecture
